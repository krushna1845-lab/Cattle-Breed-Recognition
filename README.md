# Image-based Breed Recognition App

This is a code bundle for Image-based Breed Recognition App. The original project is available at https://www.figma.com/design/whJm8VctqlYjCXemWQdYXz/Image-based-Breed-Recognition-App.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

