# Core GUI and UI libraries
customtkinter>=5.2.0
tkinter-tooltip>=2.1.0

# Image processing
Pillow>=10.0.0

# Voice recognition and text-to-speech
SpeechRecognition>=3.10.0
pyttsx3>=2.90
pyaudio>=0.2.11

# Audio processing (required for speech recognition)
portaudio19-dev  # Linux only - install via system package manager
# For Windows/Mac: pip install pyaudio should work directly

# Data handling
numpy>=1.24.0
pandas>=2.0.0

# Optional: For better audio handling
sounddevice>=0.4.6
soundfile>=0.12.1

# System utilities
pathlib
threading
dataclasses
enum34; python_version<'3.4'

# Development tools (optional)
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0